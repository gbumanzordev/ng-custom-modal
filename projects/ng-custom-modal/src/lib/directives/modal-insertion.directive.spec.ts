import { ModalInsertionDirective } from './modal-insertion.directive';

describe('ModalInsertionDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalInsertionDirective();
    expect(directive).toBeTruthy();
  });
});
